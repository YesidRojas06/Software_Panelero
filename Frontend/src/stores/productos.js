import { defineStore } from "pinia"
import axios from "axios"

export const UserStore = defineStore("producto", {
    state: () => ({
        userData: ":)",
        token: null
    }),
    actions:{
        
    }
})